-- ============================================================
-- План репетиции: что будем играть. Хранится как HTML,
-- сформированный WYSIWYG-редактором (буллеты, стили текста и т.д.).
-- ============================================================

ALTER TABLE "rehearsals"
    ADD COLUMN IF NOT EXISTS "plan" text;
