upstream nextjs {
    server nextjs:3000;
}

server {
    listen 80 default_server;
    server_name marinero.art www.marinero.art _;

    location /.well-known/acme-challenge/ {
        root /var/www/certbot;
    }

    location / {
        proxy_pass http://nextjs;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection "upgrade";
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_cache_bypass $http_upgrade;
        client_max_body_size 200m;
    }
}

# После certbot раскомментируйте и перезапустите nginx:
#
# server {
#     listen 443 ssl http2;
#     server_name marinero.art www.marinero.art;
#
#     ssl_certificate /etc/letsencrypt/live/marinero.art/fullchain.pem;
#     ssl_certificate_key /etc/letsencrypt/live/marinero.art/privkey.pem;
#
#     location / {
#         proxy_pass http://nextjs;
#         proxy_http_version 1.1;
#         proxy_set_header Upgrade $http_upgrade;
#         proxy_set_header Connection "upgrade";
#         proxy_set_header Host $host;
#         proxy_set_header X-Real-IP $remote_addr;
#         proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
#         proxy_set_header X-Forwarded-Proto $scheme;
#         proxy_cache_bypass $http_upgrade;
#         client_max_body_size 200m;
#     }
# }
#
# server {
#     listen 80;
#     server_name marinero.art www.marinero.art;
#     return 301 https://$host$request_uri;
# }
